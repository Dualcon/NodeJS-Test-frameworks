var expect    = require('chai').expect;
var assert = require('chai').assert;

describe('Mocha template.', function() {

	before(function (done) {
		done();
	});

	after(function (done) {
		done();
	});

	beforeEach(function(done) {
		done();
	});

	afterEach(function(done) {
		done();
	});

	it('Example.', function(done) {
		done();
	});

});
